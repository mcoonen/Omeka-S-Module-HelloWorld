<?php
/**
 * Variables passed in from HelloController::indexAction():
 *   $globalName   – from global 'Omeka\Settings'       (same on every site)
 *   $siteGreeting – from 'Omeka\Settings\SiteSettings' (per-site value)
 *   $showWeather  – from 'Omeka\Settings\SiteSettings' (per-site toggle)
 */
?>
<h2>Welcome to Hello World!</h2>

<p>Enter a name and push the Greet button.</p>
<div id="hello">
    <?php echo $this->partial('common/hello-form'); ?>
</div>

<hr>
<h3>Settings demo</h3>
<table>
    <tr>
        <th>Setting</th>
        <th>Value</th>
        <th>Storage</th>
    </tr>
    <tr>
        <td>Global name</td>
        <td><?php echo $this->escapeHtml($globalName); ?></td>
        <td><code>setting</code> table – shared across all sites<br>
            Configured at: Admin → Modules → Hello World (Configure)</td>
    </tr>
    <tr>
        <td>Site greeting</td>
        <td><?php echo $this->escapeHtml($siteGreeting); ?></td>
        <td><code>site_setting</code> table – unique per site<br>
            Configured at: Admin → Sites → [this site] → Settings</td>
    </tr>
    <tr>
        <td>Show weather widget</td>
        <td><?php echo $showWeather ? 'Yes' : 'No'; ?></td>
        <td><code>site_setting</code> table – unique per site</td>
    </tr>
</table>
